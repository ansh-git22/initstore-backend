spring.application.name=initstore-backend

# Server Configuration
# Railway provides PORT environment variable dynamically
server.port=${PORT:8080}

# ===============================================================
# DATABASE CONNECTION SETTINGS (POSTGRESQL)
# Uses Render database with Railway deployment
# ===============================================================

# Database connection details from Render
spring.datasource.url=jdbc:postgresql://dpg-d3elmvbuibrs73cc0bsg-a.oregon-postgres.render.com/initstore_db
spring.datasource.username=initstore_db_user
spring.datasource.password=ND0HG1DghW0jlg0y0N66ga1TSoUBGMia
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ===============================================================
# JPA / HIBERNATE SETTINGS
# ===============================================================

# 'update' automatically updates the database schema based on your @Entity classes


# 'false' in production to reduce logs
spring.jpa.show-sql=false

# Specifies the specific 'flavor' of SQL to use for PostgreSQL

# 'true' formats the SQL printed to the console to be more readable
spring.jpa.properties.hibernate.format_sql=true