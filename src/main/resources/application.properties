spring.application.name=initstore-backend

# ===============================================================
# SERVER CONFIGURATION
# ===============================================================

# Railway provides PORT dynamically; default to 8080 for local
server.port=${PORT:8080}

# ✅ Enable cross-domain session cookies (critical for Vercel + Render)
server.servlet.session.cookie.same-site=None
server.servlet.session.cookie.secure=true

# ===============================================================
# DATABASE CONNECTION SETTINGS (POSTGRESQL)
# ===============================================================

spring.datasource.url=jdbc:postgresql://SG-Initstoredb-7427-pgsql-master.servers.mongodirector.com:5432/Initstoredb
spring.datasource.username=sgpostgres
spring.datasource.password=7jd4dTcP8sg&zJYZ

# Automatically update database schema based on entities
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ===============================================================
# JPA / HIBERNATE SETTINGS
# ===============================================================

# Set to 'false' in production to reduce logs
spring.jpa.show-sql=false

# Format SQL queries for readability
spring.jpa.properties.hibernate.format_sql=true
