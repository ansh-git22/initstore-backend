spring.application.name=initstore-backend

# ===============================================================
# SERVER CONFIGURATION
# ===============================================================

server.port=${PORT:8080}

server.servlet.session.cookie.same-site=None
server.servlet.session.cookie.secure=true

# ===============================================================
# DATABASE CONFIG (SUPABASE POOLER)
# ===============================================================

# ✅ Use pooler connection (IMPORTANT)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://aws-1-ap-northeast-1.pooler.supabase.com:5432/postgres?sslmode=require}

# ⚠️ Username must include project ref
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres.gewydbvvpnzzoizzrhdj}

# ⚠️ Put your real password here for local testing
spring.datasource.password=#Varsha_143

spring.datasource.driver-class-name=org.postgresql.Driver

# ===============================================================
# JPA / HIBERNATE
# ===============================================================

spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true